/* se guardan los modulos */
export interface Canciones{
    canciones:[{
    titulo?: String, /* ? seguridad */
    artista?: String,
    duracion?: String,
    genero?: String,
    album?: String,
    src?: String
    
    }]
}